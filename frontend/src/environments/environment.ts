// Configuration d'environnement SkillMap
// apiBaseUrl pointe vers le backend Spring Boot (profil local sur le port 8081)
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8081/api',
};
